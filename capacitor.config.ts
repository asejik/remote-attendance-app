import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.abccompany.attendance',
  appName: 'AttendanceApp',
  webDir: 'dist'
};

export default config;
